import commands
#check = commands.getoutput("7_visualization.sh ../app")
#i=check.find("Error")
#if i!=-1:
#    print "already"
#else:
#    print "ng"
def test():

   c=commands.getoutput("docker ps")
   i=check.find("50100")
   if i==-1:
     print "not run"
     return
   print "docker stops"

if __name__ == '__main__':
  ret=test()